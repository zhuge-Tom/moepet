[build-system]
requires = ["maturin>=1.7,<2"]
build-backend = "maturin"

[project]
name = "moepet-memory-core"
version = "0.1.0"
requires-python = ">=3.11"

[tool.maturin]
features = ["pyo3/extension-module"]
